<Window x:Class="FanControlUI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Universal Fan Hub" Height="650" Width="500"
        Background="#121212" Foreground="#E0E0E0"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">
    
    <Window.Resources>
        <Style TargetType="Border" x:Key="CardStyle">
            <Setter Property="Background" Value="#1E1E1E"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="BorderBrush" Value="#2D2D30"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="TextBlock" x:Key="HeaderStyle">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="#888888"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardStyle}" Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="MAX CPU TEMP" Style="{StaticResource HeaderStyle}"/>
                    <TextBlock x:Name="CpuMaxTempText" Text="-- °C" FontSize="28" FontWeight="Bold" Foreground="#FF4C4C"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1">
                    <TextBlock Text="MAX GPU TEMP" Style="{StaticResource HeaderStyle}"/>
                    <TextBlock x:Name="GpuMaxTempText" Text="-- °C" FontSize="28" FontWeight="Bold" Foreground="#4CAF50"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Row="1">
            <StackPanel>
                <TextBlock Text="INDIVIDUAL SYSTEM SENSORS" Style="{StaticResource HeaderStyle}" Margin="0,0,0,10"/>
                <ItemsControl x:Name="TelemetryList">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="#2A2A2D" CornerRadius="4" Padding="10,5" Margin="0,0,10,10" MinWidth="80">
                                <StackPanel HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Name}" FontSize="11" Foreground="#AAAAAA" HorizontalAlignment="Center"/>
                                    <TextBlock Text="{Binding ValueText}" FontSize="16" FontWeight="Bold" Foreground="#FF9800" HorizontalAlignment="Center" Margin="0,2,0,0"/>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource CardStyle}" Grid.Row="2" Padding="15,15,5,15">
            <StackPanel Height="Auto">
                <TextBlock Text="HARDWARE FAN ROUTING" Style="{StaticResource HeaderStyle}" Margin="0,0,0,10"/>
                <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,0,10,0">
                    <ItemsControl x:Name="ControllersList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="#2A2A2D" CornerRadius="6" Padding="15" Margin="0,0,0,10">
                                    <StackPanel>
                                        <Grid Margin="0,0,0,10">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <TextBlock Text="{Binding Name}" FontSize="14" FontWeight="Bold" Foreground="#FFFFFF" Grid.Column="0" VerticalAlignment="Center"/>
                                            <CheckBox Content="AUTO" IsChecked="{Binding IsAuto}" Foreground="#4CAF50" FontWeight="Bold" Grid.Column="1" VerticalAlignment="Center"/>
                                        </Grid>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>
                                            <Slider Minimum="0" Maximum="100" Value="{Binding TargetSpeed}" IsEnabled="{Binding IsManual}" TickFrequency="1" IsSnapToTickEnabled="True" VerticalAlignment="Center" Margin="0,0,15,0" Grid.Column="0"/>
                                            <TextBlock Text="{Binding SpeedText}" Foreground="#5A9DFF" FontSize="16" FontWeight="Bold" MinWidth="55" TextAlignment="Right" Grid.Column="1" VerticalAlignment="Center"/>
                                        </Grid>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </StackPanel>
        </Border>
    </Grid>
</Window>